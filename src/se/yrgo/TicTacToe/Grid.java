package se.yrgo.TicTacToe;

public class Grid {
    private GridItems[][] gameBoard;

    public Grid() {
       topLeft = emptyBox;
       colDiv1 = colD

        gameBoard = {
                {topLeft, colDiv1, topMid, ColDiv2, topRight},
                {underLine1, plus1, underLine2, plus2, underLine3},
                {midLeft, colDiv3, midMid, ColDiv4, midRight},
                {underLine4, plus3, underLine5, plus4, underLine6},
                {bottomLeft, Div5, bottomMid, ColDiv6, bottomRight}
        };


    }

}
